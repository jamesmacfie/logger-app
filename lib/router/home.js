'use strict';/** * Routing for the home page. We return locations as a property of the data context for the view, * but also subscribe to all loos and datapoints for the app. */Router.route('/', {	name: 'home',	data: function() {		return {			locations: Locations.find()		};	},	waitOn: function() {		return [			Meteor.subscribe('locations'),			Meteor.subscribe('loos'),			Meteor.subscribe('dataPoints')		];	}});